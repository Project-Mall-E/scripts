# Mall-E Scripts

This repo contains the `get_store_url_and_tags` pipeline.

When running with `--store-in-database`, the default storage backend is **Supabase**. To use Firestore instead, set `STORAGE_BACKEND=firestore`.

See `get_store_url_and_tags/README.md` for full storage setup details.
